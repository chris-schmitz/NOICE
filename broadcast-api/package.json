{
  "name": "broadcast-api",
  "version": "1.0.0",
  "description": "A json api used to listen for messages from clients and broadcast them down to other clients",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
      "node": "6.3.1"
  },
  "author": "schmitz.chris@gmail.com",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.2",
    "socket.io": "^2.0.1"
  }
}
