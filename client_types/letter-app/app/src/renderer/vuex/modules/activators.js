const state = {
  words: [
    {id: 1, word: 'NOICE'},
    {id: 2, word: 'LOL'}
  ],
  patterns: [
    {id: 1, label: 'Each letter then flash'}
  ]
}

const getters = {
}

export default {
  namespaced: true,
  state,
  getters
}
